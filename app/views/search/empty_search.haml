- content_for :title do
  No Results - Search

.section--primary
  .container
    %h1
      = t('search.index.title')
      %span.sr-only &ldquo;#{@query_parameter}&rdquo;

    = render partial: 'search_box'

%section#content{:tabindex => "0"}
  .container
    = render partial: 'empty_search'
